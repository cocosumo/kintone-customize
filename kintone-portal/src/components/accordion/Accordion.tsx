import {Box} from '@mui/system';
import {AccordionContainer} from './AccordionContainer';

export const Accordion = () => {
  return (
    <AccordionContainer>
      <Box sx={{bgcolor: 'red'}}>Hello</Box>
    </AccordionContainer>
  );
};