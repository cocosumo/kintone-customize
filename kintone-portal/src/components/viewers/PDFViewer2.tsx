/* import FullScreenModal from '../modals/FullScreenModal';


const PDFViewer2 = ({isModalOpen, setIsModalOpen, url} : PDFViewerProps) => {
  console.log(url);
  return (
    <FullScreenModal {...{isModalOpen, setIsModalOpen}} >
      <iframe title="PDF" src={`https://docs.google.com/viewer?url=[${url}]&embedded=true`} />
    </FullScreenModal>);
};

export default PDFViewer2; */